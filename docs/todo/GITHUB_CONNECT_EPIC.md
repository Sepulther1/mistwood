# Epic: GitHub Connect — methodical, enforced

- [ ] Probe script: emit JSON + human lines (remote reachability, sync %, untracked, remote-only, ahead/behind).
- [ ] Dashboard: add GitHub subrows (parse probe output) under the GitHub row.
- [ ] Handoff template: add explicit “GitHub connect required” section with next actions.
- [ ] Wiki: screenshots for connector auth + indexing search trick.
- [ ] CI hook: fail handoff artifact build if connector not verified.
- [ ] Policy refresh: add `policy.version` + preflight status to all WFB headers.
