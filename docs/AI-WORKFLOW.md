# AI–Dev Working Rules

- Output MUST include: **Path**, what to paste, and a one-line **why**.
- Prefer **bash loops** to touch both repos at once.
- If a file is being replaced entirely, say **“replace file completely”**.
- Default to **Mermaid + generators** for visuals (MAP, TIMELINE).
- Add a **LOGBOOK** entry for meaningful wins.